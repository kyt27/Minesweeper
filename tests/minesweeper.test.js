import {
    relocateMines,
    increaseMineCount,
    decreaseMineCount,
    updateFlag,
    clickTile,
    revealMines,
    checkMine,
} from "../src/minesweeper.js";

test("", () => {
    expect(true).toBe(true);
});